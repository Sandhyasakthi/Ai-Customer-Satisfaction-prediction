services:
  - type: web
    name: customer-satisfaction-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: MONGO_URI
        sync: false # User will set this in the Render dashboard
      - key: PYTHON_VERSION
        value: 3.10.0
